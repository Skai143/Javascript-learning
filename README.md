The code contains the following assignment:
Write a function that takes a time in 24-hour format (e.g., 14) and returns 'Morning', 'Afternoon', 'Evening', or 'Night'.
Write a function that takes a number and returns 'Palindrome' if it reads the same forward/backward, otherwise 'Not Palindrome'.
Write a function that takes a person’s sleep hours and returns 'Too Little' (<6), 'Adequate' (6–8), or 'Too Much' (>8).
Write a function that takes a number and returns 'Divisible by 2 and 3', 'Divisible by 2 only', 'Divisible by 3 only', or 'Not divisible'.
